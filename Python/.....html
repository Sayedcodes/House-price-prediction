<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>For My Madam Jii — Princess Day</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&family=Great+Vibes&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg1:#fff0f6; --bg2:#ffeef8; --pink:#ff4da6; --soft:#ffd6e8; --accent:#ff75c7;
      --card-w:min(720px,92vw);
    }
    html,body{height:100%;margin:0;font-family:Poppins,system-ui,Segoe UI,Arial;}
    body{background:linear-gradient(180deg,var(--bg1),var(--bg2));display:flex;align-items:center;justify-content:center;padding:24px}.card{width:var(--card-w);background:linear-gradient(180deg,#ffffffcc, #fff7fb);border-radius:20px;box-shadow:0 10px 30px rgba(255,77,166,0.14);overflow:hidden;border:1px solid rgba(255,77,166,0.06)}

header{display:flex;align-items:center;gap:16px;padding:18px 22px}
.logo{width:56px;height:56px;border-radius:12px;background:linear-gradient(135deg,var(--accent),var(--pink));display:flex;align-items:center;justify-content:center;color:white;font-weight:700;font-size:20px;box-shadow:0 6px 18px rgba(255,77,166,0.18)}
h1{margin:0;font-size:18px;color:#5b2b44}
p.lead{margin:0;font-size:12px;color:#7b4a63}

.stage{display:grid;grid-template-columns:1fr 340px;gap:18px;padding:22px}
.left{padding:18px;border-radius:14px;background:linear-gradient(180deg,#fff,#fff0f7);min-height:360px;display:flex;flex-direction:column;gap:12px}

.preview{flex:1;border-radius:12px;padding:18px;background:linear-gradient(180deg,#fff6fb,#fff0f6);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;position:relative}

.big-title{font-family:'Great Vibes',cursive;font-size:36px;color:var(--pink);margin:0}
.subtitle{font-size:14px;color:#6a3b57;margin:0}

.hearts{position:absolute;inset:10px;pointer-events:none}
.heart{position:absolute;width:22px;height:22px;background:var(--pink);transform:rotate(45deg);border-radius:6px 6px 0 0;opacity:0.9}
.heart:before,.heart:after{content:'';position:absolute;background:var(--pink);width:22px;height:22px;border-radius:50%}
.heart:before{top:-11px;left:0}
.heart:after{left:11px;top:0}

.controls{display:flex;gap:8px;align-items:center}
input[type=text]{flex:1;padding:10px;border-radius:10px;border:1px solid #ffd9ee;background:#fff;outline:none}
button{padding:10px 12px;border-radius:10px;border:0;background:var(--pink);color:white;font-weight:600;cursor:pointer}

.right{padding:18px;border-radius:14px;background:linear-gradient(180deg,#fff,#fff6fb);min-height:360px;display:flex;flex-direction:column;gap:12px}
.note{font-size:14px;color:#4a2438;line-height:1.4}
.gift{background:linear-gradient(180deg,#fff0f6,#fff);border-radius:12px;padding:12px;border:1px dashed rgba(255,77,166,0.14)}

.copy{display:flex;gap:8px}
.message{background:#fff;padding:12px;border-radius:10px;border:1px solid rgba(0,0,0,0.04);min-height:72px}

footer{padding:12px 22px;background:linear-gradient(180deg,#fff0fb00,#fff0fb00);display:flex;justify-content:space-between;align-items:center}

@media(max-width:880px){
  .stage{grid-template-columns:1fr;}
  .right{order:2}
}

/* subtle floating hearts animation */
@keyframes floatUp{0%{transform:translateY(20px) scale(.8);opacity:0}50%{opacity:1}100%{transform:translateY(-140px) scale(1);opacity:0;}}

  </style>
</head>
<body>
  <div class="card">
    <header>
      <div class="logo">❤</div>
      <div>
        <h1>Princess Day — A little surprise</h1>
        <p class="lead">Cute pink card for you Madam Jii</p>
      </div>
    </header><section class="stage">
  <div class="left">
    <div class="preview" id="preview">
      <div class="hearts" id="hearts"></div>
      <h2 class="big-title" id="titleText">For Madam Jii ❤</h2>
      <p class="subtitle" id="subtitle">You make mornings brighter — happy Princess Day.</p>

      <div style="margin-top:8px;text-align:center">
        <svg width="120" height="120" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M12 21s-6-4.35-8-7.333C1.667 11.217 3.333 4 12 4s10.333 7.217 8 9.667C18 16.65 12 21 12 21z" fill="url(#g)"/>
          <defs><linearGradient id="g" x1="0" x2="1"><stop offset="0" stop-color="#ffd6e8"/><stop offset="1" stop-color="#ff8acb"/></linearGradient></defs>
        </svg>
      </div>
    </div>
  
    <div style="display:flex;gap:8px;margin-top:8px;align-items:center">
      <button id="downloadBtn">Download as PNG</button>
      <button id="surpriseBtn">Tap here how much you love me</button>
    </div>
  </div>

  <aside class="right">
    <div class="note">
      <strong>This is for you</strong>
      <ul>
        <li>You already are a princess, no doubt. <strong><em>My Moon</em></strong>
        <li>You <em>you are my everything.
        <li><em>Take a moment for yourself today.</em> Breathe in the love and joy you bring to others. You deserve it.</li>
        <li>Remember, being a princess isn't about crowns or castles. It's about kindness
      </ul>
    </div>

    <div class="gift">
      <strong>Pre-written notes for you</strong>
      <div style="margin-top:8px" class="copy">
        <div style="flex:1" class="message" id="messageBox">Bas aap zindagi mai aayi, aur sab theek lagne laga 😭❤</div>
      </div>
      <div style="display:flex;gap:8px;margin-top:8px">
        <button id="copyBtn">Copy message</button>
        <button id="altBtn">Load romantic line</button>
      </div>
    </div>

    <div style="font-size:13px;color:#6a3b57">Style: Cute pink, doodle hearts, soft fonts. Mobile first layout.</div>
  </aside>
</section>

<footer>
  <small style="color:#7b4a63">Made with care</small>
  <small style="color:#7b4a63">Your loyal <strong>"Sayed Hamza"</strong></small>
</footer>

  </div><script>
  const nameInput = document.getElementById('nameInput');
  const applyBtn = document.getElementById('applyBtn');
  const titleText = document.getElementById('titleText');
  const subtitle = document.getElementById('subtitle');
  const hearts = document.getElementById('hearts');
  const surpriseBtn = document.getElementById('surpriseBtn');
  const copyBtn = document.getElementById('copyBtn');
  const altBtn = document.getElementById('altBtn');
  const messageBox = document.getElementById('messageBox');

  applyBtn.addEventListener('click', ()=>{
    const v = nameInput.value.trim() || 'Madam Jii ❤';
    // FIXED: use template literal so variable interpolates correctly
    titleText.textContent = `For ${v}`;
    subtitle.textContent = 'You make mornings brighter — happy Princess Day.';
  });

  // small hearts generator
  function makeHeart(x,y,size,delay){
    const h = document.createElement('div');
    h.className = 'heart';
    h.style.left = x+'%';
    h.style.top = y+'%';
    h.style.width = size+'px';
    h.style.height = size+'px';
    h.style.opacity = 0;
    hearts.appendChild(h);
    // FIXED: use backticks so ${delay} is inserted properly (previously caused errors)
    h.style.animation = `floatUp 2500ms ease-in ${delay}ms forwards`;
    // ensure removal happens after animation + delay (delay is ms)
    setTimeout(()=>h.remove(), 2500 + delay + 200);
  }

  surpriseBtn.addEventListener('click', ()=>{
    // generate a few hearts and change message
    subtitle.textContent = 'A small surprise — thinking of you ❤';
    for(let i=0;i<8;i++){
      makeHeart(20 + Math.random()*60, 60 + Math.random()*10, 18 + Math.random()*10, i*120);
    }
    messageBox.textContent = 'Bas tumhara khayal aaya, aur sab theek lagne laga 🙂❤';
  });

  // copy message
  copyBtn.addEventListener('click', ()=>{
    navigator.clipboard.writeText(messageBox.textContent).then(()=>{
      copyBtn.textContent = 'Copied!';
      setTimeout(()=>copyBtn.textContent='Copy message',1200);
    });
  });

  altBtn.addEventListener('click', ()=>{
    messageBox.textContent = 'I will be here — for your bad days, your sleepy mornings, and everything in between. ❤';
  });

  // Download simple PNG using svg serialization trick for the preview area
  document.getElementById('downloadBtn').addEventListener('click', async ()=>{
    const node = document.getElementById('preview');
    try{
      const rect = node.getBoundingClientRect();
      const canvas = document.createElement('canvas');
      canvas.width = rect.width * 2; canvas.height = rect.height * 2;
      const ctx = canvas.getContext('2d');
      ctx.scale(2,2);
      // simple background
      ctx.fillStyle = getComputedStyle(node).background || '#fff';
      ctx.fillRect(0,0,rect.width,rect.height);
      // draw text
      ctx.fillStyle = '#ff4da6';
      // you can't rely on custom fonts being available in canvas immediately; fallback to a generic if necessary
      ctx.font = '36px "Great Vibes", cursive';
      ctx.textAlign = 'center';
      ctx.fillText(titleText.textContent, rect.width/2, 90);
      ctx.fillStyle = '#6a3b57';
      ctx.font = '14px Poppins, system-ui, Arial';
      ctx.fillText(subtitle.textContent, rect.width/2, 130);
      // export
      const url = canvas.toDataURL('image/png');
      const a = document.createElement('a'); a.href = url; a.download = 'princess-card.png'; a.click();
    }catch(e){
      alert('Download not supported here — take a screenshot of the preview instead.');
    }
  });

  // initial tiny hearts drop
  for(let i=0;i<4;i++) makeHeart(10 + i*20, 70 - i*6, 16+i, i*180);
</script>


</body>
</html>
